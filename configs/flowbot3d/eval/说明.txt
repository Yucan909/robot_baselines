统一评测配置生成说明

验证物体数：50
验证目标（物体+活动部件）数：56
每个目标重复：20 次
目标活动部件初始开度：总关节范围的 10%～20% 均匀随机
成功条件：最终开度达到或超过 40%

机械臂底座位姿：
- 每个目标使用 base_poses 中 selected_base.base_pose；
- 如果没有 selected_base 才使用 incumbent_base_pose；
- 本次 56 个验证目标均成功读取。

机械臂初始关节状态：
- 全部目标共用 robot_initial_pose.json 中的 9 维 initial_qpos。

相机：
- 单视角；
- 每个目标固定；
- 统一选择 repeat_2 的 camera_pose.npy；
- 原因：repeat_2 在 56/56 个验证目标中都存在，因此不需要为个别目标换规则。
- 20 次重复实验中相机位姿保持不变，只有目标活动部件的初始开度随机。

最大操作步数：
- 当前暂定 150；
- 正式全量评测前建议用正确运动方向的理想控制测试一次，确认 150 步足够从 10%～20% 打开至 40%。

输出：
- eval_pose_catalog.jsonl：56 个目标的机械臂底座位姿、机械臂初始关节状态、固定单视角相机位姿。
- evaluation_protocol.json：统一评测协议。
